import { StatChip } from '@semiconlabs/web';

export const Default = () => (
  <div className="flex flex-wrap items-center gap-8">
    <StatChip value="14" label="skill tracks" />
    <StatChip value="47" label="modules" />
    <StatChip value="107+" label="real world scenarios" />
  </div>
);
